Inventory:
Add multiple pages
Add Shields and Gems
Add sorting

Add multiple Characters, with their own loadout + stats

Rewrite projectile code - have Pattern objects handle drawing,collision,movement for all bullets they create

Add proper controls (global vars + controller support)

Fix inventory bug which crashes when inv is full
Stop player putting multiple of the same gun in loadout
Change guncard backgrounds to a nice gradient colour