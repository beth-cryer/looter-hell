draw_text(argument0,argument1,string_hash_to_newline(argument2));

//update width of current thing
update_uiwidth(argument2);

//return width just in case
return string_width(string_hash_to_newline(argument2));
